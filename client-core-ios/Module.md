# Module client-core-ios

Core iOS-specific module for Bluetooth LE client operations.

This module provides iOS-specific implementations of the generic BLE client interfaces,
wrapping CoreBluetooth framework types (`CBCentralManager`, `CBPeripheral`) into
Kotlin Coroutines-based APIs.
